# Scopus Browser Playbook

- Prefer the official Elsevier API for metadata search when available.
- Use browser workflows for institution-gated export or download paths only.
- Detect login walls and pause rather than entering credentials.
- Archive completed browser downloads into the `downloads` table with checksums.
