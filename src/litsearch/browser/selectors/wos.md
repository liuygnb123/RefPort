# Web of Science Browser Playbook

- Prefer the official API for metadata search when available.
- Use browser export only as a logged-in institutional supplement.
- Detect login walls and pause for manual user action.
- Save snapshots before export parsing so records remain traceable to a browser session.
